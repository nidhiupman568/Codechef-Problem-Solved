#include <bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here
	int t;
	cin>>t;
	while(t--){
	     int A, B, P, Q;
        cin >> A >> B >> P >> Q;
        
        cout << ((P % A == 0 && Q % B == 0) && abs(P / A - Q / B) <= 1 ? "YES\n" : "NO\n");
	}
	return 0;
}
