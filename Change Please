#include <iostream>

using namespace std;

int main() {
    int T;
    cin >> T;

    while (T--) {
        int X;
        cin >> X;
        if (X%10==0){
            cout<<(100-X)<<endl;
        } else {

        int max_payment = (100 - X ) / 10;

        cout << max_payment*10 << endl;
        }
    }

    return 0;
}
