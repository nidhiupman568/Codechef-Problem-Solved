/*
Observe that if atleast one zero can come at first place the string can always be transfomed into a string sorted in ascending order
This is can always happen except for n == 2
When n == 2 the string can not be changed
*/
#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int test_cases;
    cin >> test_cases;
    while(test_cases--){
        int n;
        cin >> n;
        string s;
        cin >> s;
        if(n == 2){
            cout << s << '\n';
        } else{
            sort(s.begin(),s.end());
            cout << s << '\n';
        }
    }
    return 0;
}
