#include <iostream>
#include <cmath>
using namespace std;

int main() {
    int testCases;
    cin >> testCases;
    
    while (testCases--) {
        int num;
        cin >> num;
        int k = ceil(num / 2.0);
        cout << k << endl;
    }
    
    return 0;
}
