#include <bits/stdc++.h>
using namespace std;

int main() {
  int t;
  cin >> t;
  while(t--) {
    int n;
    cin >> n;
    int arr[n];

    set<int> set;
    for(int i = 0; i < n; ++i) {
      cin >> arr[i];
      set.insert(arr[i]);
    }

    cout << n - set.size() << endl;

  }
  return 0;
}
