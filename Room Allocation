#include <bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here
	int t;
	cin >> t;
	while(t--){
	    int n, s = 0;
	    cin >> n;
	    int a[n];
	    for(int i = 0; i < n; i++){
	        cin >> a[i];
	        s += a[i] / 2 + a[i] % 2;
	    }
	    cout << s << endl;
	}

}
