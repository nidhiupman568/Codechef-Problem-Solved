#include <bits/stdc++.h>
using namespace std;

int main() {
 // your code goes here
 int t;
 cin>>t;
 while(t--){
     int n; cin>>n;
     int min,max;
     max = (n/2) + (n%2);
    min = (n/3) + (n%3!=0);
    
    cout<<max<<" "<<min<<endl;
 }
}
